\documentclass[12pt]{article}

\usepackage{amsmath, geometry, amssymb, bm, graphicx}
\geometry{margin=1in}
\newcommand{\dd}{\mathrm{d}}

\begin{document}
\title{cs224n a2 mzx}
\maketitle

\section*{(a)}

\[
    -\sum_{w \in \mathrm{Vocab}} y_w \log(\hat{y}_{w}) = - ( y_{o} \log (\hat{y}_{o}) + \sum_{w \in \mathrm{Vocab},\, w \neq o} y_{w} \log (\hat{y}_{w}) ) = - \log (\hat{y}_{o}), 
\]
where $y_o = 1$, $y_w = 0$ for $w \neq o$.

\section*{(b)}
\begin{align*}
    \frac{\partial J_{\mathrm{naive-softmax}}(v_c, o, U)}{\partial v_{c}} 
    &= - \frac{\partial }{\partial v_{c}} \log \mathbb{P}(O=o \mid C=c) \\
    &= - \frac{\partial }{\partial v_{c}} \log \frac{\exp(u_{o}^{\top}v_{c})}{\sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c})} \\
    &= - \frac{\partial }{\partial v_{c}} \left[ \log \exp(u_{o}^{\top}v_{c}) - \log \sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c}) \right] \\
    &= - \frac{\partial }{\partial v_{c}} \left[ u_{o}^{\top}v_{c} - \log \sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c}) \right] \\
    &= -u_{o} + \frac{\partial }{\partial v_{c}} \left[  \log \sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c}) \right] \\
    &= -u_{o} + \frac{1}{\sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c})} \frac{\partial }{\partial v_{c}} \left[ \sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c}) \right] \\
    &= -u_{o} + \frac{1}{\sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c})}  \left[ \sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c}) \frac{\partial }{\partial v_{c}} (u_{w}^{\top}v_{c}) \right] \\
    &= -u_{o} + \frac{1}{\sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c})}  \left[ \sum_{w \in \mathrm{Vocab}} \exp(u_{w}^{\top}v_{c}) u_{w} \right] \\
    &= -u_{o} +  \sum_{w \in \mathrm{Vocab}} \frac{\exp(u_{w}^{\top}v_{c}) u_{w}}{\sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top}v_{c})}\\
    &= -u_{o} + \sum_{w \in \mathrm{Vocab}}\mathbb{P}( O=w \mid C=c )u_{w}\\
    &= -u_{o} + \sum_{w \in \mathrm{Vocab}}\hat{y}_{w}u_{w}\\
    &= -y_{o}u_{o} + \sum_{w \in \mathrm{Vocab}}\hat{y}_{w}u_{w}\\
    &= - \sum_{x \in \mathrm{Vocab}}y_{x}u_{x} + \sum_{w \in \mathrm{Vocab}}\hat{y}_{w}u_{w}\\
    &= \sum_{x \in \mathrm{Vocab}}(\hat{y}_x - y_{x})u_{x} \\
    &= U(\hat{y} - y)
\end{align*}

\section*{(c)}

\textbf{Case 1: $w \neq o$}
\begin{align*}
    \frac{\partial J_{\mathrm{naive-softmax}}(v_c, o, U)}{\partial u_{w}}
    &= - \frac{\partial }{\partial u_{w}} \left[ u_{o}^{\top}v_{c} - \log \sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top}v_{c}) \right] \\
    &= \frac{\partial }{ \partial u_{w}} \left[\log \sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})\right] \\
    &= \frac{1}{\sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})} \frac{\partial }{ \partial u_{w}} \left[ \sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})\right] \\
    &= \frac{1}{\sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})}  \left[ \sum_{x \in \mathrm{Vocab}} \frac{\partial }{ \partial u_{w}} \exp(u_{x}^{\top} v_{c})\right] \\
    &= \frac{1}{\sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})}  \left[ \sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c}) \frac{\partial }{ \partial u_{w}} (u_{x}^{\top} v_{c})\right]\\
    &= \frac{1}{\sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})}  \exp(u_{w}^{\top} v_{c}) \frac{\partial }{ \partial u_{w}} (u_{w}^{\top} v_{c}) \\
    &= \frac{\exp(u_{w}^{\top} v_{c}) }{\sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top} v_{c})} v_{c} \\
    &= \hat{y}_{w} v_{c}
\end{align*}
\textbf{Case 2: $w = o$}
\begin{align*}
    \frac{\partial J_{\mathrm{naive-softmax}}(v_c, o, U)}{\partial u_{w}}
    &= - \frac{\partial }{\partial u_{w}} \left[ u_{o}^{\top}v_{c} - \log \sum_{x \in \mathrm{Vocab}} \exp(u_{x}^{\top}v_{c}) \right] \\
    &= - \frac{\partial }{\partial u_{w}} ( u_{o}^{\top}v_{c}) +  \hat{y}_{w} v_{c} \\
    &= -v_{c} +  \hat{y}_{w} v_{c} \\
    &= ( \hat{y}_{w} - 1)v_{c}
\end{align*}

\medskip
\noindent Thus,
\[
    \frac{\partial J_{\mathrm{naive-softmax}}}{\partial u_w} =
    \begin{cases}
        (\hat{y}_o - 1) \, v_c, & w = o \\
        \hat{y}_w \, v_c,       & w \neq o
    \end{cases}
\]

\noindent Equivalently, since $ y_{o} = 1 $ and $ y_{w} = 0$ ($w \neq o$),
this can be written as: 
\[
    \frac{\partial J_{\mathrm{naive-softmax}}}{\partial u_w} = (\hat{y}_w - y_{w}) v_c.
\] 
 


\section*{(d)}

\begin{align*}
    \frac{\dd}{\dd x} \sigma(x) 
    &= \frac{\dd}{\dd x} \frac{1}{1 + \exp(-x)} \\
    &= - (1 + \exp(-x))^{-2} \frac{\dd}{\dd x} (1 + \exp(-x)) \\
    &= - (1 + \exp(-x))^{-2} \frac{\dd}{\dd x} (\exp(-x))\\
    &= - (1 + \exp(-x))^{-2} \exp(-x) \frac{\dd}{\dd x} (-x) \\
    &= \frac{\exp(-x) }{(1 + \exp(-x))^{2}} \\
    &= \frac{1}{1 + \exp(-x)} \cdot \frac{\exp(-x)}{1 + \exp(-x)} \\
      &= \sigma(x) \cdot (1 - \sigma(x))
\end{align*}

\section*{(e)}

\begin{align*}
      \frac{\partial}{\partial z} \log \sigma(z)
      &= \frac{1}{\sigma(z)} \cdot \sigma(z)(1 - \sigma(z)) = 1 - \sigma(z) \\
      \frac{\partial}{\partial z} \log \sigma(-z)
      &= \frac{1}{\sigma(-z)} \cdot \sigma(-z)(1 - \sigma(-z)) \cdot (-1) =\sigma(-z) - 1=  -\sigma(z)
\end{align*}

\textbf{(1) $\partial J / \partial v_c$:}

\begin{align*}
    \frac{\partial}{\partial v_c} J_{\mathrm{neg-sample}}(v_{c}, o, U) 
    &= - \frac{\partial}{\partial v_c}  \left[\log(\sigma(u_{o}^{\top}v_{c})) + \sum_{k=1}^{K}\log(\sigma(-u_{k}^{\top}v_{c}))\right] \\
    &=  - \left[  [1 - \sigma(u_{o}^{\top}v_{c}) ] \frac{\partial}{\partial v_c} u_{o}^{\top}v_{c}  +  \frac{\partial}{\partial v_c} \sum_{k=1}^{K}\log(\sigma(-u_{k}^{\top}v_{c}))\right] \\
    &=  [ \sigma(u_{o}^{\top}v_{c}) - 1 ] u_{o} - \sum_{k=1}^{K}  \frac{\partial}{\partial v_c} \log(\sigma(-u_{k}^{\top}v_{c})) \\
    &= [ \sigma(u_{o}^{\top}v_{c}) - 1 ] u_{o} - \sum_{k=1}^{K} -\sigma(u_{k}^{\top}v_{c}) \frac{\partial}{\partial v_c}( u_{k}^{\top}v_{c}) \\
    &= [ \sigma(u_{o}^{\top}v_{c}) - 1 ] u_{o} + \sum_{k=1}^{K} \sigma(u_{k}^{\top}v_{c})  u_{k}
\end{align*}


\textbf{(2) $\partial J / \partial u_o$:}
\begin{align*}
    \frac{\partial}{\partial u_o} J_{\mathrm{neg-sample}}(v_{c}, o, U)
    &= - \frac{\partial}{\partial u_o}  \left[\log(\sigma(u_{o}^{\top}v_{c})) + \sum_{k=1}^{K}\log(\sigma(-u_{k}^{\top}v_{c}))\right] \\
    &= - \frac{\partial}{\partial u_o} \log(\sigma(u_{o}^{\top}v_{c})) \\
    &=  [ \sigma(u_{o}^{\top}v_{c}) - 1 ] v_{c}
\end{align*}

\textbf{(3) $\partial J / \partial u_k$ (for some $k \in [1, K]$):}
\begin{align*}
    \frac{\partial}{\partial u_k} J_{\mathrm{neg-sample}}(v_{c}, o, U)
    &= - \frac{\partial}{\partial u_k}  \left[\log(\sigma(u_{o}^{\top}v_{c})) + \sum_{k'=1}^{K}\log(\sigma(-u_{k'}^{\top}v_{c}))\right] \\
    &= - \frac{\partial}{\partial u_k}  \sum_{k'=1}^{K}\log(\sigma(-u_{k'}^{\top}v_{c}))
\end{align*}
\noindent Notice that $\dfrac{\partial(u_{k'}^{\top}v_{c})}{\partial u_k} = v_{c}$ when $k' = k$, otherwise it is $0$.
\begin{align*}
    \frac{\partial J}{\partial u_k}
    &= - \left[-\sigma(u_{k}^{\top}v_{c}) \cdot v_c\right] \\
    &= \sigma(u_{k}^{\top}v_{c}) v_c
\end{align*}
 negative sampling loss only involves $K+1$ dot products. 
 naive softmax requires summing over the entire vocabulary 

\section*{(f)}

\noindent \textbf{(1)}
\[
    \frac{\partial J_{\mathrm{skip-gram}}(v_{c},w_{t-m},...w_{t+m}, U)}{\partial U} = \sum_{-m \le j \le m ,j \neq 0} \frac{\partial J(v_{c}, w_{t+j}, U) }{\partial U}
\]

\noindent \textbf{(2)}
\[
    \frac{\partial J_{\mathrm{skip-gram}}(v_{c},w_{t-m},...w_{t+m}, U)}{\partial v_c} = \sum_{-m \le j \le m ,j \neq 0} \frac{\partial J(v_{c}, w_{t+j}, U) }{\partial v_c}
\]
\noindent \textbf{(3)}
\[
    \frac{\partial J_{\mathrm{skip-gram}}(v_{c},w_{t-m},...w_{t+m}, U)}{\partial v_w} = \sum_{-m \le j \le m ,j \neq 0} \frac{\partial J(v_{c}, w_{t+j}, U) }{\partial v_w} =  \sum_{-m \le j \le m ,j \neq 0} 0 = 0
\]

\section*{Coding (c): word vectors visualization}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{word_vectors.png}
    \caption{word vectors}
\end{figure}

\end{document}
